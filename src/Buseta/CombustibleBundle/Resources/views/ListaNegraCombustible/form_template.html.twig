<form id="{{ form.vars.id }}" name="{{ form.vars.full_name }}" action="{{ form.vars.action }}" method="POST" enctype="multipart/form-data">
    {% if form.vars.method == 'PUT' %}
        <input type="hidden" name="_method" value="PUT"/>
    {% endif %}
    {{ form_errors(form) }}
    {{ form_row(form._token) }}

    <div class="row">
        <div class="col-md-4">
            {{ form_row(form.autobus) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.fechaInicio) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.fechaFinal) }}
        </div>
    </div>

    <br/>

    {# buttons group #}
    <div class="row">
        <div class="col-md-4">
            <button type="submit" class="btn btn-primary">
                <span class="glyphicon glyphicon-save"></span> Aceptar</button>
            <div class="btn-group">
                <a class="btn btn-default" href="{{ path('listaNegraCombustible') }}">
                    <span class="glyphicon glyphicon-chevron-left"></span> Cancelar</a>
            </div>
        </div>
    </div>

</form>
